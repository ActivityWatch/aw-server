
def check_if_encrypted(data: Union[dict, str]):
    pass

def encrypt(data: Union[dict, str]):
    # Which Key Derivation Function?
    pass


def EncryptedSession(Session):
    # Ensures that all data received is encrypted, otherwise sends a warning and encrypts when received (with which key?)

